# Migratory Time 2.0 实施锚点

## 目标系统状态

Migratory Time 成为面向人和 Agent 的全球民用时间换算工具。网页、MCP、
Codex 插件和实验性 Capability 适配器共用一个确定性核心，可处理全球 IANA
时区、当地名称搜索、夏令时重复与不存在时刻、分钟精度边界、旧分享链接兼容
和有界结构化结果。

飞书云文档小组件保留源码，但标记为暂缓集成，不属于 2.0 完成线，也不参与
主产品发布结论。

## 完成线

- 网页默认仍显示现有五个常用地区，同时可搜索、添加、移除和持久化全球地区。
- `convert_time` 和 `current_times` 对合法 IANA 标识、稳定别名和无歧义地点名称
  保持一次调用；真正歧义返回候选而不猜测。
- `search_time_zones` 紧凑返回候选；旧 `list_time_zones` 作为兼容入口保留，
  不一次倾倒完整全球目录。
- MCP 成功分支使用严格判别联合；错误包含稳定结构化代码、字段、输入和候选。
- 旧五地区别名、偏好和分享短码继续可读；新分享链接支持任意 IANA 时区。
- IANA/CLDR 元数据来源、运行时规则数据版本和分钟精度限制可被检查。
- Capability Profile 保持 provider-seeded experimental，只包含规范 IANA 换算语义，
  不吸收产品标签、搜索、复制文本或分享链接。
- 当前源码通过开发回归；新安装插件在冷会话中通过普通中英文请求选择正确工具，
  记录调用次数且不使用 Web、Shell 或模型手算；网页通过桌面、手机、键盘、分享、
  持久化、错误恢复和离线复验。

## 产品与交互对象

- `时区`：规范 IANA 标识、国家/地区、主要城市、稳定别名和本地化显示名称。
- `显示地区`：用户当前列表中有序且唯一的时区集合，至少一项。
- `时刻`：实时或固定的 `Temporal.Instant`。
- `当地时间输入`：来源时区中的日期时间，可能有效、重复、不存在或超出分钟精度。
- `分享快照`：固定时刻和有序显示地区；语言仍属于查看者偏好。

地区选择器是页面级对话框。搜索只缩小候选，不改变选择；选择动作立即更新列表和
本地偏好；关闭、`Esc` 和点击遮罩返回原页面操作点。默认列表不增加说明面板。

## Agent 路由预算

- 普通“现在几点”或明确换算：一个领域工具调用。
- 无歧义城市、国家主时区、现有中英文别名：不先搜索。
- 地点或缩写存在真实歧义：一次结果返回候选，由用户选择后再调用。
- 显式询问支持项或探索地点：一次 `search_time_zones`。
- 非法输入：一次稳定错误，不重试，不回退到通用工具。

## 非目标

- 节假日、工作日、日出日落、航班、日历账号、会议写入和提醒。
- 跨提供者替代性声明；当前只有一个 provider-seeded experimental Profile。
- 飞书上传、租户、管理员或市场审核。
- 为全球目录增加独立管理后台、账户或云数据库。

## 当前状态

- 基线：`main` 与 `origin/main` 一致，开始实施前工作树干净。
- 已完成：全球注册表、共享核心、旧链接兼容、网页搜索与有序偏好、四工具 MCP、
  封闭判别结果、结构化错误、有界输出和 v0.2 实验性 Capability 适配器。
- 已完成：本地 personal marketplace 与安装缓存更新到
  `2.0.0+codex.20260820152701`；普通中文当前时间和英文全球换算各自主路由为一次
  正确 MCP 调用，无 Web、搜索或计算 Shell 回退；该门禁固定使用
  `gpt-5.6-luna` / `low`，`UTC-5` 类显式偏移也保持
  一次调用且不猜测缺失的正负号。
- 已完成：桌面、390px 手机、768px 中间宽度、键盘关闭、全球搜索、顺序持久化、
  新分享链接、Chatham 重复时刻、控制台和断网重载验收。
- 已完成：人类 UI 必要入口复核。默认和全球地区在中英文主列表中均保留明确地点
  身份；五个悬浮入口在桌面悬停/键盘聚焦时显示短标签，触屏不残留标签；320px
  长地点名自然换行，481–760px 短高度模式为纵向悬浮操作预留内容安全区。地区选择器
  不再重复功能说明和品牌署名，固定偏移在主列表与选择器中也不重复显示同一偏移。
- 发布状态：本地 2.0 构建、已安装插件和浏览器流程已就绪；本轮按所有者授权
  创建一次本地 Git 提交。GitHub Pages 仍是旧构建，部署一致性检查按预期 FAIL；
  未推送或部署。
- 下一动作：如需公开新版，另行推送/发布；发布后重新执行
  `npm run check:deployments`。

## 验证阶梯

```text
npm test
npm run build:web
npm run typecheck:mcp
npm run check:mcp
npm run capability:check
npm run check:codex-plugin
npm run check:deployments
```

浏览器验收使用线上等价本地构建，覆盖窄屏、中间宽度、宽屏、键盘、重复/不存在
当地时间、全球地区搜索、旧/新分享链接、偏好恢复和断网重载。

## 恢复方式

继续实施时先读取本文件、`docs/product-model.md`、`capabilities/README.md`，再查看
`git diff --stat`、`git diff --name-only` 和本文件“当前状态”。任何旧检查结果都需在
当前源码重新运行后才能用于完成结论。
